
  # cleanloop

  This is a code bundle for cleanloop. The original project is available at https://www.figma.com/design/V36e1YWI2KEHRxq1yhNF8w/cleanloop.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  